name: agree
baseline:
  - name: b1
    composite: pkg.composites.x
inputs:
  - {artifact: upstream-a, from: upstream-a}
  - {artifact: upstream-b, from: upstream-b}
parent_studies:
  - upstream-a
  - {study: upstream-b, condition: ran}    # already covered by inputs.from
# Legacy parents ⊆ canonical inputs.from → fires `dag_edges_legacy_redundant` (warning).
