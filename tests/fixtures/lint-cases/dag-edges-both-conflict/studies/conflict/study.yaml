name: conflict
baseline:
  - name: b1
    composite: pkg.composites.x
inputs:
  - {artifact: upstream-a, from: upstream-a}
pipeline_gate:
  prerequisites:
    - upstream-z   # NOT in inputs.from → silently ignored downstream
# Legacy has a slug absent from canonical inputs.from →
# fires `dag_edges_legacy_and_canonical_disagree` (warning).
